= form_tag(:controller => 'duplicates', :action => 'create', :method => 'post') do
  %legend Copy Pages to New Locality
  = select_tag "to_locality", options_from_collection_for_select(@locality, "id", "name")
  .container
    = submit_tag "Copy to New Locality", class: 'btn btn-primary btn-page-save'
    .container 
      .details{style: "color: #AAA; font-style:italic; margin-top: 10px;"} Notes: 
      .details{style: "color: #AAA; font-style:italic;"} This will only copy the pages in your current locality which you haven't already copied to the desired locality. 
      .details{style: "color: #AAA; font-style:italic;"} It will also take some time to process depending on how many pages you are copying.  
      .details{style: "color: #AAA; font-style:italic;"} Please be patient.
